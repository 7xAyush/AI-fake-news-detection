
  # AI Fake News Detection App

  This is a code bundle for AI Fake News Detection App. The original project is available at https://www.figma.com/design/yPRtFmHLU3mH1W3NFwD0q5/AI-Fake-News-Detection-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  <!-- To run everything on your side:

Restart backend (new model already saved):

In a new PowerShell window:
cd "C:\Users\ayush\Desktop\New folder"
python backend/app.py
You should see it listening on http://127.0.0.1:5000.
(If needed) Restart frontend:

Open another PowerShell:
cd "C:\Users\ayush\Desktop\New folder\frontend"
npm run dev
Open the URL Vite prints (likely http://localhost:4173/). -->